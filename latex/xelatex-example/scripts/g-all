#!/usr/bin/env bash

set -euo pipefail

for f in *.tex; do
  echo "==================================================================="
  echo "==== ${f} ===="
  [[ $f != emoji-colour.tex ]] && g ${f}
  echo "==================================================================="
done
